---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: bliss
  namespace: bliss
spec:
  interval: 5m
  chart:
    spec:
      # renovate: registryUrl=https://bjw-s.github.io/helm-charts
      chart: app-template
      version: 1.5.1
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      interval: 5m
  values:
    env:
      TZ : America/Chicago
    image:
      pullPolicy: Always
      repository: cooperised/bliss
      tag: latest@sha256:9dabb5a8bf229750cf5a08db8d2fc7319f7eca9cea14e760a92140b6799e3374
    ingress:
      main:
        enabled: true
        annotations:
          cert-manager.io/cluster-issuer: letsencrypt
          external-dns.alpha.kubernetes.io/internal-hostname: bliss.sievert.fun
        hosts:
          - host: bliss.sievert.fun
            paths:
              - path: /
                pathType: Prefix
        tls:
          - hosts:
              - bliss.sievert.fun
            secretName: bliss-cert
    persistence:
      config:
        enabled: true
        mountPath: /config
      media:
        enabled: true
        mountPath: /media
        existingClaim: bliss-media
    service:
      main:
        ports:
          http:
            port: 3220
